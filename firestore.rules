rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
    match /users/{userId} {
      // Prototype mode: Allows read/write based on path parameter
      allow read, write: if true;
    }
  }
}
